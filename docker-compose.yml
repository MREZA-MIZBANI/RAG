version: '3.8'

services:
  web:
    build: .
    container_name: llm_qa_app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./media:/app/media
      - ./db.sqlite3:/app/db.sqlite3
    env_file:
      - .env
    restart: always